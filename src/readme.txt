This was generated with the command 
nest g res buddies --no-spec
Use it to go fast into the exam, in case its required that you make a new repository.

Installations:
    npm install --save @nestjs/typeorm typeorm pg
    npm install class-validator class-transformer
    npm install --save @nestjs/config



Run the app command:
    yarn start run:dev
